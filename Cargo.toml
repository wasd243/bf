[package]
name = "bf"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"]}
libc = "1.0.0-alpha"
#inkwell = { version = "0.10", features = ["llvm22-1"]}

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
